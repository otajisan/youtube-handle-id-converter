# cp .env.example .env して値を埋める。.env は git 管理外
# YouTube Data API v3 の API Key(必須。未設定だと backend が起動しない)
YOUTUBE_API_KEY=

# Cloud Logging 形式の JSON ログを出す場合は gcp を指定(ローカルでは通常不要)
# SPRING_PROFILES_ACTIVE=gcp

spring:
  application:
    name: youtube-handle-id-converter

server:
  # Cloud Run は PORT 環境変数でリッスンポートを指定する
  port: ${PORT:8080}
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

# cp .env.example .env.local して使う(.env.local は git 管理外)
# backend の URL。ローカルは docker compose / bootRun の 8180、本番は Cloud Run の URL(Repository variable から注入)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8180

# GitHub Pages の project site 用 basePath。ローカルで / 直下に配信したいときは空にする
# NEXT_PUBLIC_BASE_PATH=
